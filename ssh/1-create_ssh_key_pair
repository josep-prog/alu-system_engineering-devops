#!/usr/bin/env bash

# Ensure .ssh directory exists
mkdir -p ~/.ssh

# Generate the RSA key pair with 4096 bits and passphrase 'betty'
ssh-keygen -t rsa -b 4096 -f ~/.ssh/school -N "betty" -q

# Fix permissions of the private key
chmod 600 ~/.ssh/school

echo "SSH key pair created successfully."
